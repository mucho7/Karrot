type Post {
  id: ID!
  title: String!
  content: String!
  status: PostStatus!
  author: User!
}

enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

input CreatePostInput {
  title: String!
  content: String!
  authorId: ID!
  status: PostStatus! = DRAFT
}
